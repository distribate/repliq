{
  "name": "database",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "gen-types": "dotenv -e .env npx supabase gen types --lang typescript --db-url %POSTGRE_URL% > ../../packages/types/entities/gen-supabase.ts",
    "dump-seed": "dotenv -e .env npx supabase db dump --db-url %POSTGRE_URL% -f supabase/seed.sql --data-only",
    "dump-schema": "dotenv -e .env npx supabase db dump --db-url %POSTGRE_URL% -f supabase/schema.sql"
  },
  "devDependencies": {
    "supabase": "1.219.2"
  }
}
