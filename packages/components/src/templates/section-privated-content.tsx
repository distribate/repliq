import { Typography } from '@repo/ui/src/components/typography.tsx';

export const SectionPrivatedContent = () => {
  return (
    <Typography className="text-shark-300">
      Видно только вам.
    </Typography>
  )
}